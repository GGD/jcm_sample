<table>
  <thead>
    <tr>
      <th>Ttile</th>
      <th>PubDate</th>
    </tr>
  </thead>

  <tbody>
    <% @feeds.each do |feed| %>
      <tr>
        <td><%= feed['title'] %></td>
        <td><%= feed['pubDate'] %></td>
      </tr>
    <% end %>
  </tbody>
</table>
